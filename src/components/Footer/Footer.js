import React from "react";
import s from './Footer.module.css';

function Footer () {
    return (
        <div className={s.footer}>
            footer
        </div>
    );
}

export default Footer;