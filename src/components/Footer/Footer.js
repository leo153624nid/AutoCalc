import React from "react";
import s from './Footer.module.css';

function Footer () {
    return (
        <div className={s.footer}>
            <span>footer</span>
        </div>
    );
}

export default Footer;